@model IEnumerable<UniversityAutomate.Areas.Admin.ViewModels.GroupAdminDTO>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GroupName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UniversityName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GroupName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UniversityName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-groupId="@item.GroupID">Edit</a> |
                <a asp-action="Details" asp-route-groupId="@item.GroupID">Details</a> |
                <a asp-action="Delete" asp-route-groupId="@item.GroupID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
