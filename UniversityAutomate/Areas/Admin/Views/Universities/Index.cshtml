@model IEnumerable<UniversityAutomate.Areas.Admin.Models.University>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UniversityName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.City)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UniversityName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.City.CityID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-universityId="@item.UniversityID">Edit</a> |
                <a asp-action="Details" asp-route-universityId="@item.UniversityID">Details</a> |
                <a asp-action="Delete" asp-route-universityId="@item.UniversityID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
